# Improvement Backlog

## High Priority

- Add a dedicated `Checks` sheet with model status, formula-error checks and key tie-outs.
- Add a `Sources` or `Audit` sheet for assumptions, market benchmarks and valuation multiples.
- Standardize workbook language across all sheets and screenshots.
- Rename chart series from default labels like `Series 1`, `Series 2`, `Series 3`.
- Add clear unit labels to all output tables, for example `RUB`, `RUB/month`, `%`, `x`.

## Medium Priority

- Add a change log or version note on the `Cover` sheet.
- Add period labels that clearly separate forecast years and months.
- Add sensitivity for discount rate and revenue multiple.
- Add a scenario comparison output table with Conservative, Base and Optimistic side by side.
- Add a break-even month output and explain the operating driver behind it.

## Low Priority

- Export a PDF preview of the model for reviewers without Excel.
- Add a short walkthrough GIF or screen recording.
- Add CSV exports for key annual outputs.
- Improve dashboard chart formatting and remove selected-cell artifacts from screenshots.

## Analyst-Grade Next Version

For a more advanced version, the model could include:

- bottom-up acquisition funnel
- cohort retention logic
- monthly marketing spend by channel
- marketplace supply/demand balance
- funding requirement and runway analysis
- terminal value bridge
- valuation sensitivity tables
- data-backed market assumptions with sources
